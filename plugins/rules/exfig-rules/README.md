# exfig-rules

Coding conventions for projects using ExFig. Auto-loaded when the plugin is installed.

## Conventions

- Never manually edit ExFig-generated files
- Validate config before committing
- Use PKL config patterns (local variables, for-generators)
- Platform-specific naming conventions
- CI/CD best practices
